spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/j4mb_ledger
    username: j4mb
    password: j4mb123
  docker:
    compose:
      enabled: false
  # Disable Flyway in tests — no real database is required for context load tests.
  # Integration tests that need a real DB should use Testcontainers.
  flyway:
    enabled: false
  jpa:
    hibernate:
      # Skip schema validation in tests — no live DB.
      ddl-auto: none
